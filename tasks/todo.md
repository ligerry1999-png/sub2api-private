# Sub2API v0.1.165 私有升级执行清单

> 开始日期：2026-07-27
>
> 本分支只在独立工作目录中升级。未经单独授权，不执行 `deploy=true`，不修改生产配置，不在生产服务器构建。

## 已固定的基线

- [x] 官方稳定标签：`v0.1.165`
- [x] 官方基线提交：`e9a58c1cb8b5ef626a75c93b4d953fde5e67aa29`
- [x] 当前生产私有真源：`origin/main` 的 `e680938b6c8330eb7755b0354060c99ffe3a12a1`
- [x] 本轮分支：`upgrade/v0.1.165-private`
- [x] 本轮独立工作目录：`/Users/bricoleur/Documents/小红书 cil/sub2api-upgrade-v0.1.165-private`

## 执行原则

- 公开 `v0.1.165` 是新底座，私有功能按原始提交顺序逐批迁移。
- 不把公开仓库 `main` 的未发布提交混入本轮。
- 每批只处理一个功能主题；冲突必须根据新旧代码行为人工融合，不能机械选择一侧覆盖另一侧。
- 每批完成后检查差异、测试和私有接口，再开始下一批。
- 当前公开版已有的安全、Codex/Responses 和对象存储修复必须保留。
- 当前私有版的接口、角色、图片日志和部署保护必须保留。
- 先完成“升级到 v0.1.165 且保持线上行为”，再用独立提交接对象存储；两个风险不能绑在一起。

## 第一阶段：迁移护栏

- [x] 记录 `v0.1.157..origin/main` 的私有提交顺序和依赖关系
- [x] 确认以下私有测试在升级分支中最终存在：
  - `/v1/image-jobs/*` 路由与状态机测试
  - `result_delivery=file_url` 与默认 Base64 测试
  - ChatGPT2API 图片 worker 失败分类与切换测试
  - 图片日志、缩略图和清理测试
  - `account_manager` 权限边界测试
  - Codex Responses 流式与图片结果测试
- [x] 记录上游 `v0.1.165` 已有的关键修复，迁移私有代码时不得回退
- [x] 保持 `backend/migrations/136_image_logs.sql` 的文件名和内容不变
- [x] 增加从生产式 v0.1.157 数据库升级到 v0.1.165 的迁移演练
- [x] 验证数据库升级后旧 v0.1.157 镜像能否安全回滚

## 第二阶段：逐批迁移私有功能

- [x] 图片 worker 桥接与失败切换
- [x] 图片日志、缩略图和 7 天清理
- [x] 私有 `/v1/image-jobs/*`、失败分类和取消接口
- [x] `account_manager` 受限账号管理员角色
- [x] `result_delivery=file_url` 与默认 Base64 兼容
- [x] Team/Workspace 信息、自定义模型和 Codex 兼容补丁
- [x] Nginx 大图上传、存储清理和 GitHub Actions 部署保护
- [x] 构建提交溯源和维护文档

## 第三阶段：对象存储私有适配

- [ ] 保留公开版 `service.ImageStorage` 和后台配置能力
- [ ] 让私有 `/v1/image-jobs/*` 可选把最终图片上传到 S3 兼容存储
- [ ] 保持私有接口路径、任务状态、失败分类、取消接口不变
- [ ] 保持未传 `result_delivery=file_url` 时的 Base64 行为
- [ ] 默认使用私有存储桶和限时签名 URL
- [ ] 增加上传失败、链接过期和清理策略测试

## 第四阶段：验证

- [x] `git diff --check`
- [x] Go 相关轻量测试和完整单元测试
- [x] 前端类型检查
- [x] 前端相关测试
- [x] 私有路由注册检查
- [x] 对比官方 `v0.1.165`，确认关键上游修复没有被旧私有代码覆盖
- [x] 推送升级分支
- [ ] GitHub Actions 后端 CI
- [x] GitHub Actions 安全扫描
- [ ] `Deploy Server` 使用 `deploy=false` 完成构建验证
- [x] 部署脚本在重启前生成并校验真实 PostgreSQL 自定义格式备份
- [x] 明确新镜像健康失败时的旧镜像恢复步骤；正式执行仍以数据库演练通过为前提

## 第五阶段：部署门禁

- [ ] 汇总改动、风险、测试结果和回滚点
- [x] 单独取得 `deploy=true` 授权
- [ ] 部署前确认线上图片任务空闲、CPU、内存和磁盘正常
- [ ] 部署后验证版本 SHA、健康状态、私有路由和生图真实请求

## 第六阶段：现有系统默认分组保护与正式部署

- [x] 确认 SIMPLE 模式会在生产数据恢复件中新增 6 个未绑定账号的默认分组
- [x] 设计为仅在全新数据库中创建默认分组，已有分组历史的系统保持原样
- [x] 增加“全新安装仍创建、现有安装不创建、软删除历史不重建”的回归测试
- [ ] 完成本地检查并推送升级分支
- [ ] 等待 CI、安全扫描和 `deploy=false` 构建验证全部通过
- [ ] 对最终待部署提交重新执行生产数据库迁移与旧镜像回滚演练
- [ ] 保护升级前 `main` 并合并升级分支
- [ ] 通过 GitHub Actions 执行 `deploy=true`
- [ ] 核验线上 SHA、健康、私有生图路由和真实生图请求

## Review

2026-07-27 已完成生产数据库只读导出、候选版本迁移、旧生产镜像回滚和再次升级演练。演练发现候选版本会给已有 SIMPLE 模式数据库新增 6 个空默认分组，因此正式部署前增加现有系统保护。针对性后端普通测试和 PostgreSQL 18 集成测试已通过；全新安装、已有安装和软删除历史三条路径均有回归覆盖。
