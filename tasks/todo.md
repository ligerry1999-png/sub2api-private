# Sub2API v0.2.4 私有升级与生图冷却修复

> 开始日期：2026-09-09
>
> 目标：合并官方 `v0.2.4` 的 Image 2.5、OAuth 图片辅助模型和 429 修复，同时保留私人异步生图队列、支付、图片日志、ChatGPT2API worker/fallback、管理员权限和 GitHub Actions 部署保护；图片相关冷却最多 45 秒，普通文本模型冷却规则不变。

- [x] 从线上精确 SHA `1d2cbd56a9ec0ad2fbf6446a20ebfcc35b1f480e` 建立升级保护分支
- [x] 合并官方 `v0.2.4`，人工解决 VERSION、图片辅助模型和网关私有路由冲突
- [x] 恢复并核对私人支付、异步 image-jobs、图片日志、ChatGPT2API 和 Tanzhongyu 部署文件
- [x] 将 OAuth 图片 worker、图片 429、图片能力丢失和图片模型 404 冷却统一限制为最多 45 秒
- [x] 迁移旧 `ModelsListConfig` 引用到官方 `ModelAllowlist`，保留旧类型兼容层
- [ ] 本地轻量检查、前端检查和私有护栏通过
- [ ] 推送私有仓库并等待精确 SHA 的 CI、安全扫描、数据库演练通过
- [ ] 仅通过 GitHub Actions `deploy=true` 部署，服务器不构建
- [ ] 上线后核对版本、健康状态、私人路由、支付、队列和图片冷却

## Review

- 当前线上尚未修改；候选分支仍处于未提交合并状态。
- 本机没有系统级 Go 1.27，后端完整测试交由 GitHub Actions；本地先完成脚本、前端和静态检查。

---

# 生图链路可验证性修复（2026-09-14）

> 目标：在不保存 Key、Cookie、Authorization 或完整提示词的前提下，关联异步 image job 的入站参数、实际出站 payload 摘要、上游结果元数据和原始图片 Alpha 检查结果。

- [x] 为图片日志补充安全的入站/出站摘要、任务关联和哈希
- [x] 对原始图片记录真实 MIME、模式、Alpha 范围和透明像素标记
- [x] 增加 background/output_format 透传、结果检查和并发隔离测试
- [x] 区分异步提交接口与内部执行接口，并记录实际命中的上游账号类型/模型
- [x] 支持原生 Images 回退 Responses 时记录每次出站参数，识别嵌套 image_generation 工具字段
- [x] 补充调色板 PNG 透明通道识别和“入站/出站一致但结果透明”的阶段定位
- [x] 运行差异检查和可用的静态检查；记录 Go 工具链缺失或残余风险
- [ ] 提交候选代码并将同一精确提交推送到私有 GitHub `main`
- [ ] 等待该提交的 CI、安全扫描和生产数据库隔离演练全部通过
- [x] 部署前只读确认生产图片任务空闲、容器健康和资源充足
- [ ] 仅通过 GitHub Actions `Deploy Server / deploy=true` 正式部署
- [ ] 部署后核对线上提交、镜像、健康状态、重启次数和私有路由

## Review

- 已在 `openai_images_trace.go` 增加入站/生效/出站字段白名单、请求体 SHA-256、image job ID、结果元数据和原图 Alpha 统计；原图不重新编码。
- `image_logs.metadata` 新增 `received`、`effective`、`forwarded`/`forwarded_attempts`、`result`、`upstream`、`parameter_verification`、`image_verification`；每张原图记录真实 MIME、模式、Alpha 范围和 SHA-256。
- 异步任务会同时记录内容工厂实际提交的 `/v1/image-jobs/...` 与内部执行的 `/v1/images/...`，避免把两个阶段混为一谈。
- 增加原生 Images `background` 透传、Responses 回退嵌套参数、请求体读取后恢复、并发 payload 隔离、调色板 PNG 和透明阶段定位回归测试。
- `git diff --check` 通过；本机没有 Go 工具链，`go test`/`gofmt` 未能运行，未进行生产部署或真实上游请求。
- 部署前只读检查通过：线上提交仍为 `9e468b03d3a868b2d0d983d4b995953d885db27b`，Sub2API、PostgreSQL、Redis 健康，图片任务队列空闲，磁盘和内存满足部署条件。

# Sub2API v0.1.183 GitHub 发布执行

> 发布原则：只在 GitHub Runner 构建镜像；生产服务器只备份数据库、加载预构建镜像并使用 `--no-build` 重启，不在服务器编译源码。
>
> 额度策略：本地候选已完成全量单元测试和前后端构建，因此将同一候选提交快进到 `main`，只触发一轮 CI、安全扫描和数据库演练；不额外运行会重复构建的 `deploy=false`。

- [x] 将本地候选快进推送到 GitHub `main`
- [x] 修复 GitHub Security Scan 报出的 `golang.org/x/image` WebP 解码漏洞并同步升级护栏
- [x] 修复数据库演练中后台聚合清理造成的快照误报并重新推送
- [x] 修复数据库演练恢复阶段把已存在迁移误判为旧 schema，并重新完成精确提交的数据库隔离演练
- [x] 并行完成修复后提交的 CI、安全扫描和精确提交的数据库隔离演练
- [x] 确认本轮只读备份和 GitHub Runner 构建，服务器没有执行 `docker build`、`go build` 或前端构建
- [ ] 仅触发一次 `Deploy Server / deploy=true`，由 GitHub 构建镜像
- [ ] 核对线上提交、版本、健康状态、容器重启次数和私有路由
- [ ] 记录 GitHub Actions 运行编号、结果和发布后状态

## 发布 Review

- 首次 GitHub 门禁发现 `golang.org/x/image v0.43.0` 的 WebP 解码漏洞 `GO-2026-6222`，已停止继续部署；已修复并通过安全扫描。
- 数据库演练首次失败是后台聚合默认清理了备份中两条过期 `usage_logs`，并非迁移破坏数据；已让隔离演练关闭后台聚合/清理，再重新验证。
- 第二次演练在候选版通过后，旧版 `0.1.176-private.1` 的健康检查仍失败；原始日志显示旧进程约 74 秒后退出，但 Bash `set -e` 让函数内诊断没有输出。已把失败捕获移到函数外层，下一轮只为拿到容器退出码/错误字段，未绕过旧版回滚门禁，也未部署生产。
- 第三次演练拿到根因：迁移 220 已在生产库执行过，但其后一个非 Grok 分组存在合法的新视频价格；演练错误地要求当前值必须为 0。现已改为“迁移已存在时验证价格不被本次演练改写，迁移首次执行时才验证清空”。
- 第四次演练通过迁移 220 检查后，在迁移 221 的 `model_pricing` 初始化断言处发现同类误报；已准备对迁移 221 的两个字段做“已存在则保留、首次执行才检查默认值”的快照校验。
- 第五次演练中候选版和旧版都打印了正确版本号，说明镜像构建与启动成功；但后段裸 `test` 失败没有标签，日志只有 `exit code 1`。已将回滚、恢复、迁移计数和索引检查统一改为带名称的断言，下一轮可直接定位真实数据差异。
- 第六次演练定位到恢复断言误报：生产备份已包含迁移 221 的两列，脚本却固定要求恢复后列数为 0。现改为比较恢复前后的 schema 哈希，保留对迁移首次执行路径的单独校验。
- 第七次演练发现整份 `pg_dump` 文本哈希会受 PostgreSQL 恢复后的内部元数据顺序影响，产生不稳定差异；现改为按 `information_schema` 表、列、默认值和迁移文件排序后的规范化哈希。
- 第八次演练通过：提交 `96c480da1` 的 CI、Security Scan、Database Rehearsal 全部成功；演练完成升级、旧版回滚、原库恢复和再次升级，且 Runner 已清理生产备份。

## 2026-08-26 最终门禁

- CI：GitHub Actions run `32921036489`，success。
- Security Scan：GitHub Actions run `32921036501`，success。
- Database Rehearsal：GitHub Actions run `32921036507`，success。
- 精确候选 SHA：`96c480da1cb865cddf7ddbb626cb8f6cbc5c72ac`。
- 生产部署：未执行；服务器仍运行原版本。

---

# Sub2API v0.1.183 私有升级执行清单

> 开始日期：2026-08-25
>
> 当前私有基线：`0.1.176-private.1` / `fd6d81557c064e4d79d800e78b3356d7649c6e45`
>
> 目标：升级到官方稳定版 `v0.1.183`，接收 Codex 指纹默认关闭、出站身份统一、回合状态、额度探针和 429 额度耗尽调度修复，同时完整保留私有持久生图队列、图片日志和部署保护。

## 边界与审计

- [x] 从当前私有精确 SHA 建立隔离分支 `upgrade/v0.1.183-private`
- [x] 同步官方 tags，确认 `v0.1.183` 是最新稳定版
- [x] 记录官方 802 个变更文件、私有 150 个变更文件及 58 个重叠文件
- [x] 确认指纹主动开启、Codex 身份统一、turn-state、session-id 和额度耗尽 429 修复均已进入稳定版
- [x] 审核迁移 222-230、Go 1.27 和 OAuth 出站插件系统的部署影响

## 私有功能护栏

- [x] 保留 `/v1/image-jobs/*` 持久队列、取消、重试、重启恢复及 440 条假上游验收
- [x] 保留 `image_logs`、缩略图、七天清理、启动注入自检和 Base64/`file_url` 兼容
- [x] 保留 ChatGPT2API worker/fallback、Codex Responses、Team/Workspace、审核和管理员权限
- [x] 保留 GitHub Actions 构建、服务器 `--no-build`、数据库备份/失败回滚和 Nginx 大图配置
- [x] 保持 Grok 与 OpenAI 图片链路分离，跨厂商模型映射严格默认关闭

## 合并与验证

- [x] 合并官方 `v0.1.183`，所有冲突和语义重叠人工取行为并集
- [x] 更新私有版本号与升级保护脚本，重新生成必要代码
- [x] 运行 Codex 指纹默认关闭、身份、turn-state、session-id、额度探针和 429 调度定向测试
- [x] 运行私有路由、队列、日志、Base64/`file_url`、取消/重试/恢复定向测试
- [x] 运行 `git diff --check`、私有升级护栏、Go unit 全量测试和前端 lint/typecheck/test/build
- [x] 使用假上游完成 440 条队列验收，禁止产生真实生图费用
- [x] 记录本机 Docker 未启动而未完成的 integration、数据库隔离演练、GitHub CI 和生产核验项

## 发布边界

- [x] 不推送、不合并回 `main`、不部署生产，等待人工确认候选结果
- [x] 正式发布时仅通过 GitHub Actions 部署，服务器不拉源码、不构建

## Review

- 候选版已完整合并官方 `v0.1.183`，12 处硬冲突均按官方新能力与私有功能的行为并集解决；版本更新为 `0.1.183-private.1`。
- Codex 指纹收敛保持默认关闭，同时接收出站身份统一、turn-state、session-id、额度探针和 429 额度耗尽暂停/调度修复。
- 私有持久生图队列、图片日志、Base64/`file_url`、取消/重试/重启恢复、ChatGPT2API fallback 和部署保护均已保留。
- 本地 Go unit 全量测试通过；前端 244 个测试文件、1751 条测试全部通过；前后端正式构建、私有升级保护及 440 条无网络入队测试通过。
- 本机 Docker Desktop 未启动，因此 integration 中需要 Redis/PostgreSQL 容器的测试和数据库隔离演练待 GitHub CI 执行；候选分支未推送、未合并 `main`、未部署生产。

---

# Sub2API v0.1.176 私有升级执行清单

> 开始日期：2026-08-13
>
> 当前生产基线：`0.1.173-private.1` / `6daba2f2ef011e9e4590400a7710947e0ac93f87`
>
> 目标：合并官方 `v0.1.175`、`v0.1.176` 的 Codex 稳定性和 Grok 4.6 能力，同时完整保留私有持久生图队列、图片日志、Codex、Team/Workspace、审核、部署保护及图片结果兼容行为。

## 边界与私有功能护栏

- [x] 从生产精确 SHA 建立独立候选分支 `upgrade/v0.1.176-private`
- [x] 只读核对本地、GitHub、线上版本和运行状态
- [x] 合并前记录官方 107 个提交、194 个变更文件及 19 个私有重叠文件
- [x] 保留私有 `/v1/image-jobs/*` 持久队列、取消/重试/重启恢复和 440 条假请求验收
- [x] 保留 `image_logs`、缩略图、七天清理、启动注入自检及 Base64/`file_url` 兼容
- [x] 保留 Codex Responses、Team/Workspace、账号接管、审核与管理员权限边界
- [x] 保留 GitHub 构建、服务器 `--no-build` 部署、备份、健康失败自动回滚及 Nginx 大图配置

## 官方升级与 Grok 路由

- [x] 合并官方 `v0.1.176`，所有重叠文件人工取并集，不机械选择任一侧
- [x] 更新私有版本号为 `0.1.176-private.1` 并重新生成 Wire 文件
- [x] 保留 Grok 4.6、图片、视频、Voice、Search/`x_search` 能力
- [x] Codex 只允许显式选择 Grok 模型；跨厂商静默映射继续严格默认关闭
- [x] 不把现有 OpenAI 私有持久生图队列错误改成 Grok worker；两条媒体链路分别验收

## 验证与发布门禁

- [x] 运行冲突定向测试、后端 unit/integration 完整测试和前端 lint/typecheck/全量测试/构建
- [x] 使用假上游验证 440 条异步任务全部接收，不产生真实生图/视频费用
- [ ] 在无外网隔离环境演练生产备份的旧版→新版→恢复旧库→再升级
- [ ] GitHub CI、安全扫描和 `deploy=false` build-only 全部通过
- [ ] 部署前核对在途图片任务、数据库、Redis、磁盘、回滚镜像和备用 OpenAI 账号
- [ ] 仅通过 GitHub Actions 部署；服务器不拉源码、不构建

## Review

- 候选版已合并官方 107 个提交；硬冲突 2 处、语义重叠 19 处全部取并集，私有独有文件保持不变。
- 新增 Grok/composite 的 Codex 模型清单适配；一把综合 Key 可按 `gpt-5.6-sol` / `grok-4.6` 精确分流，不允许跨厂商静默改写。
- 本地已通过 Go unit/integration 全量测试、440 条无网络入队、30/50 执行槽位、前端 222 个文件/1551 条测试与前后端构建。
- 正式部署不再只信任手填 SHA；工作流会通过 GitHub Actions API 确认同一精确提交存在成功的 Database Rehearsal 记录。
- 生产数据库隔离演练、GitHub CI/安全扫描/build-only 和正式部署待候选提交推送后执行。

---

# Sub2API v0.1.173 私有升级执行清单

> 开始日期：2026-08-10
>
> 当前生产基线：`0.1.169-private.3` / `9a6db0bec32c9c605d17856065a74197f140de38`
>
> 目标：先把官方 `v0.1.172` 做成可回退安全检查点，再叠加官方 `v0.1.173` 的 Grok、渠道监控和生图修复；完整保留私有持久 image-jobs、图片日志、Codex、Team/Workspace、审核、部署保护和 Base64/file_url 兼容行为。

## 基线与隔离

- [x] 读取服务器交接、生图专题文档和历史 lessons
- [x] 只读核对本地、私有 GitHub、线上提交与健康状态
- [x] 从 `9a6db0bec` 建立独立升级工作区与分支
- [x] 保存升级前 `main` 保护分支，确认回滚镜像和 release commit 可追溯

## 第一检查点：v0.1.172

- [x] 合并官方 `v0.1.172`，人工解决冲突
- [x] 保留 OAuth 账号接管修复、Codex 容量降载换号、网络超时和图片模型冷却修复
- [x] 保留私有 image-jobs、图片日志启动自检、Codex 和部署保护
- [x] 完成针对性测试并形成可独立上线的安全检查点

## 最终候选：v0.1.173-private.1

- [x] 叠加官方 `v0.1.173`，人工融合 Grok、渠道监控 V2 和生图计费修复
- [x] 保留 Grok OAuth、图片、视频、Voice、搜索、模型映射和计费能力，默认不启用跨厂商暗默映射
- [x] 审核迁移 194–220，确认迁移 220 的数据快照和回滚办法
- [x] 更新私有版本号、Wire 生成代码和维护文档

## 验证门禁

- [x] `git diff --check`、私有功能差异审计与秘密扫描
- [x] 后端目标测试、完整测试、竞态检查和静态检查
- [x] 前端类型检查、测试和构建
- [x] 假上游 440 条异步任务全部接收，不访问真实生图服务
- [x] 验证私有路由、Base64/file_url、日志、清理、取消、重试和重启恢复
- [x] 验证 OAuth 登录修复、管理员刷新令牌、Codex 换号与 Grok 媒体路由
- [ ] 用生产数据库只读备份在隔离环境完成“旧版 → 新版 → 旧版 → 新版”演练
- [ ] GitHub CI、安全扫描和 `deploy=false` build-only 全部通过

## 正式部署与核验

- [ ] 部署前确认图片任务空闲、队列状态、数据库、Redis、磁盘和回滚点
- [ ] 合并并推送私有 GitHub `main`
- [ ] 仅通过 GitHub Actions `deploy=true` 部署，服务器不构建
- [ ] 核对线上 SHA、镜像、版本、健康、重启次数和 Nginx
- [ ] 验证私有 image-jobs 鉴权探针、图片日志启动标记和 Codex 请求
- [ ] 更新交接文档、本清单 Review 与 lessons

## Review

- 2026-08-10 本地候选：Go 全量 unit、关键竞态检查和 `go vet` 通过；前端 lint/typecheck/build 通过，220 个测试文件、1533 条测试通过；440 条假上游入队与 30/50 槽位测试通过。
- 候选版将 Grok 功能“休眠上线”：生产不创建 Grok 分组/账号，跨厂商模型映射严格显式启用，原始邮箱密码登录永久关闭。
- 数据库演练改为无外网的内部 Docker 网络，并新增迁移 220 的备份、清理、真正恢复原库与再升级校验。
- GitHub CI、安全扫描、真实生产备份演练、build-only 和正式部署待候选提交推送后执行。

---

# 生图日志注入修复与启动自检

> 开始日期：2026-08-04
>
> 根因：`2f46c00ef` 重排 Wire 生成代码时遗漏 `OpenAIGatewayService` 的 `ImageLogService` 注入，导致生图成功但日志静默跳过。

## 实现

- [x] 恢复 `OpenAIGatewayService` 与 `ImageLogService` 的连接
- [x] 改为 Wire 专用 provider 强制注入，避免人工 setter 再次遗漏
- [x] 增加启动自检：缺少日志服务时拒绝启动，成功时输出明确日志
- [x] 增加自动测试，覆盖缺失注入与正常注入
- [x] 更新私有版本号和 Wire 生成代码
- [x] 修正数据库演练的无迁移误报，并让候选启动演练检查生图日志连接成功标记

## 验证与上线

- [ ] 运行针对性测试、完整 CI、安全扫描和 build-only
- [ ] 推送 GitHub `main` 前复核私有生图路由与日志代码差异
- [ ] 部署前等待在途生图任务清空
- [ ] 通过 GitHub Actions 部署，服务器不构建
- [ ] 线上核对版本、健康、启动自检日志和新增生图日志

## Review

- 2026-08-04：首轮 CI、Security Scan 和 build-only 已通过。数据库演练确认候选版本能够启动并通过健康检查，但旧脚本把“本次没有新增迁移”误判为失败；现已按候选与线上版本是否实际改动迁移文件分别校验。
- 正式部署和线上新增生图日志验证待最终门禁通过后填写。

---

# 私有 image-jobs 持久队列改造

> 开始日期：2026-08-02
>
> 目标：合法异步生图请求先持久化并返回 `202 + job_id`，后台继续复用现有用户/账号 Redis 并发槽位；不再用固定 4 个进程槽位充当接收上限。
>
> 验证边界：440 条批量验收只能使用假上游，禁止向真实生图账号发起 40/50/440 并发请求；生产服务器禁止构建。

## 实现

- [x] 原子保存任务元数据、请求正文和安全请求元数据，不落盘完整 Key
- [x] 增加独立 Redis ready/delayed/active 队列、幂等入队和失败恢复
- [x] 用有界 dispatcher 领取任务，真实执行继续服从用户与账号并发槽位
- [x] 本地容量暂满、内部并发 429、暂时无账号时重新排队，不把任务标成最终失败
- [x] 支持 pending/running 取消、重启恢复和过期 lease 回收
- [x] 结果改为有界/流式落盘，保持 Base64 与 `result_delivery=file_url` 兼容
- [x] 清理逻辑只删除终态任务，不删除 pending/running
- [x] 保持现有 create/get/result/cancel 路由和响应字段兼容

## 配置与部署

- [x] 增加队列条数、字节容量、worker ceiling、重试与 lease 配置
- [x] 把配置传入生产 Docker Compose，废弃无效的 `ASYNC_IMAGE_MAX_CONCURRENT=4` 接收门禁
- [x] 部署时暂停领取新任务，只等待 running，保留 pending 并在重启后恢复
- [ ] 加入队列深度、最老任务、重试、worker 和账号槽位监控
- [x] 记录旧版本回滚时 pending 队列的兼容处理

## 验证

- [x] 单元测试覆盖 202 接收、队列满、取消、重试、幂等和重启恢复
- [x] 假上游模拟 440 条提交全部返回 202，不发生真实生图
- [x] 假账号并发 50 时真实执行峰值不超过 50 且能够达到 50
- [x] 用户并发低于账号并发时执行峰值取较小值，任务继续 pending 而非失败
- [ ] 运行前端/后端相关测试、完整 CI、安全扫描和 build-only
- [ ] 上线前检查数据库、Redis、磁盘、活跃图片任务与回滚点

## Review

- 2026-08-02：本机使用 Go 1.26.5 和 golangci-lint 2.9.0 通过针对性 handler、repository、Wire 编译测试和完整代码扫描；全部生图执行器均为内存假实现，没有访问真实上游。
- 完整 GitHub CI、安全扫描、build-only 和生产上线前只读检查仍按门禁分别确认，未获得正式部署授权前不合并 `main`、不运行 `deploy=true`。

---

# Sub2API v0.1.169 安全热修与私有升级执行清单

> 开始日期：2026-08-02
>
> 当前生产基线：`0.1.165-private.2` / `6f9805cd870434f4fa666ccb6779f9ede84be599`
>
> 第一阶段只移植官方 `017f6bb` 路径护栏与容器禁止提权，不夹带新功能；第二阶段再从官方稳定标签 `v0.1.169` 建立干净升级分支。所有生产部署都经 GitHub Actions，禁止在服务器构建。

## 第一阶段：`0.1.165-private.2` 紧急安全热修

- [x] 从当前 `origin/main` 建立独立热修分支
- [x] 移植 `/responses/*subpath` 三条入口的闭集路径护栏
- [x] 在 service 层对路径提取与拼接做二次校验
- [x] 同步 Gemini 模型路径与 Grok 视频 request ID 的同类护栏
- [x] 为生产 compose 增加 `security_opt: no-new-privileges:true`
- [x] 保留私有 `/responses/compact`、Codex direct、Grok 与生图路由行为
- [x] 增加恶意编码、点路径、过长/过深路径和合法子路径回归测试
- [x] 运行 `git diff --check`、后端目标测试、前端/部署轻量检查
- [x] 推送热修分支并等待 GitHub CI、安全扫描与 `deploy=false` 构建通过
- [x] 确认热修零数据库迁移；部署前生成并校验生产备份，沿用已通过的 v0.1.165 回滚演练
- [x] 合并到私有 GitHub `main`，通过 GitHub Actions `deploy=true` 部署
- [x] 部署后核验精确 SHA、健康、重启次数、Nginx、Codex Responses 与真实生图

## 第二阶段：`0.1.169-private.1` 完整升级

- [x] 从官方稳定标签 `v0.1.169` 建立干净升级分支
- [x] 合并官方底座与 105 个私有改动文件并人工解决文本冲突
- [ ] 重新生成 Wire 依赖注入文件并用 `go mod tidy` 校准依赖锁定文件
- [x] 阻止图片日志路径逃逸 `dataDir`，覆盖读取与删除两条路径
- [x] 让图片 worker 冷却时仍可按声明回退到 native 生图
- [x] 给私有 image-jobs 增加真正生效的有界执行队列，防止协程和内存耗尽
- [x] 让 `account_manager` 使用 Passkey 登录时与密码/OAuth 权限一致
- [ ] 合并用户/API Key 声明列更新，避免并发覆盖余额和用量
- [ ] 合并面板 API 限流与系统设置部分更新修复
- [ ] 合并图像依赖安全升级、计费统计和账号故障切换修复
- [ ] 合并代理断流熔断 fail-open 修复与 GPT-5.6 费率更新
- [ ] 评估并按需启用 Kimi K3；Passkey 与模型广场默认不启用
- [ ] 完成数据库迁移/回滚、全套 CI、私有功能和真实生图回归
- [ ] 通过 GitHub Actions 部署并保留可验证回滚镜像

## 本轮 Review

- 紧急安全热修已通过 GitHub Actions 部署，生产版本、镜像与提交精确一致；公网、Responses 和真实生图请求通过。
- 完整升级候选保持 Passkey、模型广场默认关闭，Kimi K3 只保留可配置能力，不自动启用。
- 最终部署前必须让候选提交通过 CI、安全扫描、build-only 和生产数据库恢复件的“升级 → 旧版回滚 → 再升级”演练。

---

# 历史：Sub2API v0.1.165 私有升级执行清单

> 开始日期：2026-07-27
>
> 本分支只在独立工作目录中升级。未经单独授权，不执行 `deploy=true`，不修改生产配置，不在生产服务器构建。

## 已固定的基线

- [x] 官方稳定标签：`v0.1.165`
- [x] 官方基线提交：`e9a58c1cb8b5ef626a75c93b4d953fde5e67aa29`
- [x] 当前生产私有真源：`origin/main` 的 `e680938b6c8330eb7755b0354060c99ffe3a12a1`
- [x] 本轮分支：`upgrade/v0.1.165-private`
- [x] 本轮独立工作目录：`/Users/bricoleur/Documents/小红书 cil/sub2api-upgrade-v0.1.165-private`

## 执行原则

- 公开 `v0.1.165` 是新底座，私有功能按原始提交顺序逐批迁移。
- 不把公开仓库 `main` 的未发布提交混入本轮。
- 每批只处理一个功能主题；冲突必须根据新旧代码行为人工融合，不能机械选择一侧覆盖另一侧。
- 每批完成后检查差异、测试和私有接口，再开始下一批。
- 当前公开版已有的安全、Codex/Responses 和对象存储修复必须保留。
- 当前私有版的接口、角色、图片日志和部署保护必须保留。
- 先完成“升级到 v0.1.165 且保持线上行为”，再用独立提交接对象存储；两个风险不能绑在一起。

## 第一阶段：迁移护栏

- [x] 记录 `v0.1.157..origin/main` 的私有提交顺序和依赖关系
- [x] 确认以下私有测试在升级分支中最终存在：
  - `/v1/image-jobs/*` 路由与状态机测试
  - `result_delivery=file_url` 与默认 Base64 测试
  - ChatGPT2API 图片 worker 失败分类与切换测试
  - 图片日志、缩略图和清理测试
  - `account_manager` 权限边界测试
  - Codex Responses 流式与图片结果测试
- [x] 记录上游 `v0.1.165` 已有的关键修复，迁移私有代码时不得回退
- [x] 保持 `backend/migrations/136_image_logs.sql` 的文件名和内容不变
- [x] 增加从生产式 v0.1.157 数据库升级到 v0.1.165 的迁移演练
- [x] 验证数据库升级后旧 v0.1.157 镜像能否安全回滚

## 第二阶段：逐批迁移私有功能

- [x] 图片 worker 桥接与失败切换
- [x] 图片日志、缩略图和 7 天清理
- [x] 私有 `/v1/image-jobs/*`、失败分类和取消接口
- [x] `account_manager` 受限账号管理员角色
- [x] `result_delivery=file_url` 与默认 Base64 兼容
- [x] Team/Workspace 信息、自定义模型和 Codex 兼容补丁
- [x] Nginx 大图上传、存储清理和 GitHub Actions 部署保护
- [x] 构建提交溯源和维护文档

## 第三阶段：对象存储私有适配

- [ ] 保留公开版 `service.ImageStorage` 和后台配置能力
- [ ] 让私有 `/v1/image-jobs/*` 可选把最终图片上传到 S3 兼容存储
- [ ] 保持私有接口路径、任务状态、失败分类、取消接口不变
- [ ] 保持未传 `result_delivery=file_url` 时的 Base64 行为
- [ ] 默认使用私有存储桶和限时签名 URL
- [ ] 增加上传失败、链接过期和清理策略测试

## 第四阶段：验证

- [x] `git diff --check`
- [x] Go 相关轻量测试和完整单元测试
- [x] 前端类型检查
- [x] 前端相关测试
- [x] 私有路由注册检查
- [x] 对比官方 `v0.1.165`，确认关键上游修复没有被旧私有代码覆盖
- [x] 推送升级分支
- [ ] GitHub Actions 后端 CI
- [x] GitHub Actions 安全扫描
- [ ] `Deploy Server` 使用 `deploy=false` 完成构建验证
- [x] 部署脚本在重启前生成并校验真实 PostgreSQL 自定义格式备份
- [x] 明确新镜像健康失败时的旧镜像恢复步骤；正式执行仍以数据库演练通过为前提

## 第五阶段：部署门禁

- [ ] 汇总改动、风险、测试结果和回滚点
- [x] 单独取得 `deploy=true` 授权
- [ ] 部署前确认线上图片任务空闲、CPU、内存和磁盘正常
- [ ] 部署后验证版本 SHA、健康状态、私有路由和生图真实请求

## 第六阶段：现有系统默认分组保护与正式部署

- [x] 确认 SIMPLE 模式会在生产数据恢复件中新增 6 个未绑定账号的默认分组
- [x] 设计为仅在全新数据库中创建默认分组，已有分组历史的系统保持原样
- [x] 增加“全新安装仍创建、现有安装不创建、软删除历史不重建”的回归测试
- [ ] 完成本地检查并推送升级分支
- [ ] 等待 CI、安全扫描和 `deploy=false` 构建验证全部通过
- [ ] 对最终待部署提交重新执行生产数据库迁移与旧镜像回滚演练
- [ ] 保护升级前 `main` 并合并升级分支
- [ ] 通过 GitHub Actions 执行 `deploy=true`
- [ ] 核验线上 SHA、健康、私有生图路由和真实生图请求

## Review

2026-07-27 已完成生产数据库只读导出、候选版本迁移、旧生产镜像回滚和再次升级演练。演练发现候选版本会给已有 SIMPLE 模式数据库新增 6 个空默认分组，因此正式部署前增加现有系统保护。针对性后端普通测试和 PostgreSQL 18 集成测试已通过；全新安装、已有安装和软删除历史三条路径均有回归覆盖。
# Sub2API 最新上游升级与 GPT-6 计费审计

> 开始日期：2026-09-06
>
> 当前生产基线：`0.1.183-private.1` / `3fd1354d6283cb0fcb2949147da152b81b959c9d`
>
> 官方目标：`v0.2.1` / `578785ee7fb35030b094b69624efe25670a36f5f`
>
> 目标：核对官方最新稳定版与新增能力，审计 `gpt-6-astra` 和 `gpt-5.6-*` 的记录、归类、定价及汇总差异；在保留全部私有生图与部署保护的前提下升级并发布。

## 基线与差异

- [x] 获取官方最新 release、`main` 和变更记录，确认目标版本
- [x] 核对本地、私有 GitHub、生产 SHA、健康和资源状态
- [x] 记录官方改动、私有改动及重叠文件，建立私有功能护栏
- [ ] 确认部署前无运行中的图片任务和不可中断业务

## GPT-6 统计与计费

- [x] 追踪 `gpt-6-astra` 的 requested/upstream/response model 记录链路
- [x] 对比 `gpt-5.6-sol/terra/luna` 的模型归类、价格匹配与汇总逻辑
- [x] 核对生产分组价格、实际 usage log 和后台统计展示
- [x] 对未知价格、别名、映射和静默回退增加明确保护或测试

## 升级、验证与发布

- [x] 从当前私有精确 SHA 建立隔离升级分支和升级前保护分支
- [x] 人工合并官方稳定版，并保留 image-jobs、图片日志、Codex、审核和部署保护
- [x] 更新私有版本、生成代码和必要文档
- [x] 运行后端定向/全量测试、前端检查和私有护栏；远端安全扫描待候选提交推送
- [ ] 完成生产数据库无外网隔离演练与精确 SHA 门禁
- [ ] 仅通过 GitHub Actions 构建和部署，服务器不构建
- [ ] 核对线上版本、健康、私有路由、GPT-6 请求与计费记录
- [ ] 填写本节 Review 和维护教训

## 已确认的 GPT-6 计费基线

- 后台按精确模型名分别汇总；`gpt-6-astra` 不会并入 `gpt-5.6-*`。
- 生产已有 16 次 `gpt-6-astra` 成功调用，requested/upstream/response model 一致，总实际费用 `2.283320 USD`。
- Astra 标准价：输入 `$10/M`、缓存读 `$1/M`、缓存写 `$12.5/M`、输出 `$50/M`。
- `gpt-5.6-sol` 标准价：`$5 / $0.5 / $6.25 / $30`；`terra`：`$2 / $0.2 / $2.5 / $12`；`luna`：`$0.2 / $0.02 / $0.25 / $1.2`。
- `v0.2.1` 新增 Astra 本地兜底价，并对超过 272,000 输入 token 的 Astra 请求应用长上下文倍率：输入/缓存费用 `2x`、输出 `1.5x`。
- Fast/Priority 为 `2x`；官方 `v0.2.1` 将 Ultrafast 纳入同一服务层级计费。

## Review

- 2026-09-06：已将官方 `v0.2.1` 人工合入私有候选，6 处硬冲突均按官方新能力与私有行为的并集解决；私有版本为 `0.2.1-private.1`。
- 私有 `/v1/image-jobs/*`、图片日志、Base64/`file_url`、Codex 模型清单、账号管理角色及 GitHub Runner 部署保护均已保留。
- 本地后端全量单元测试通过；首次出现的 3 条阿里云验证码超时由本机代理接管 `127.0.0.1` 测试请求导致，清除代理变量后全部通过。前端 254 个测试文件、1863 条测试、类型检查和正式构建通过。
- Astra 增加 Ultrafast `2x` 回归测试；远程价格目录不可用时，`v0.2.1` 的本地兜底价仍能正确计费。远端 CI、安全扫描、数据库演练和正式部署结果待发布后补齐。

---
