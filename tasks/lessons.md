- 2026-08-26: 数据库升级演练启动完整应用时，应用默认的后台聚合/保留策略可能改写真实备份中的旧 usage_logs，导致“迁移前后行数必须相同”的检查出现假失败。隔离演练要关闭所有会主动清理或聚合历史数据的后台任务，只验证 schema、迁移、回滚和私有配置；生产运行配置不能因此关闭清理。
- 2026-08-26: GitHub Security Scan 实际发现 `golang.org/x/image v0.43.0` 的 WebP 解码过量内存分配漏洞（GO-2026-6222），本地普通测试通过不能替代漏洞扫描。以后涉及图片解码链路的依赖升级必须同时跑 govulncheck；遇到真实漏洞先修复再部署，不能以“上游已有漏洞”或“仅影响恶意图片”为理由绕过门禁。
- 2026-08-26: 数据库演练脚本里的裸 `test` 在 `set -e` 下失败时只会给出 `exit code 1`，无法判断是回滚快照、迁移计数还是恢复表结构出错。所有关键门禁都必须使用带业务名称的断言函数，失败时输出 expected/actual，不能依赖 shell 默认错误信息。
- 2026-08-26: 生产备份不一定对应最初的旧 schema；已执行过的迁移后仍可能有合法业务数据。恢复演练不能固定断言某些新列/新表不存在，应比较恢复前后的 schema 快照，并只对“本次首次执行的迁移”验证默认值或清空行为。
- 2026-08-26: 不能直接对整份 `pg_dump` 文本做 schema 哈希来判断恢复一致性；PostgreSQL 恢复后内部对象顺序或元数据文本可能变化。应从 `information_schema` 提取排序后的结构字段做规范化比较，并用关键迁移断言补充覆盖。
- 2026-08-26: 用户要求升级时严禁在生产服务器构建，并希望减少 GitHub Actions 额度。以后已在本地完整验证的私有升级，优先把同一候选 SHA 快进到 `main`，让 CI、安全扫描和数据库演练并行且只跑一轮；不要先在候选分支跑全套、再因 `main` 推送重复跑，也不要额外执行与正式部署重复构建的 `deploy=false`。正式部署只允许 GitHub Runner 构建，服务器执行备份、`docker load` 和 `docker compose --no-build`。
- 2026-08-13: 用户再次强调官方升级不能遗失私有功能。以后每次升级都必须先从“当前线上精确 SHA”生成私有功能护栏清单，再审计官方变更与私有改动的重叠文件；冲突必须取行为并集，并用私有路由、持久队列、日志注入、部署回滚等测试证明保留，不能仅凭合并成功或首页健康判断。
- 2026-08-13: 自定义中转站的普通 `/v1/models` 列表不等于 Codex 桌面端需要的 models manifest。如果要让综合分组里的 OpenAI/Grok 模型稳定出现在 Codex 选择器，服务端必须在 `client_version` 请求下返回 `models[].slug` 形状，并按分组实际账号只列出可用文本模型；同时严禁把 `gpt-*` 静默改写成 Grok。
- 2026-08-13: 部署表单要求手填“演练 SHA”不等于真的发布门禁；正式部署必须使用只读 GitHub Actions API 核对该精确 SHA 确实存在成功的生产数据库隔离演练，否则人工输入仅是声明，不是证据。
- 2026-08-13: 启动门禁不应依赖 `docker logs` 里的固定字符串，日志系统升级会改变输出时机或位置并造成假失败。对必需依赖应使用 Wire 编译约束、服务启动 fail-fast 和健康端点三层验证，日志只做观测证据，不做唯一判定。
- 2026-08-04: 数据库演练不能写死“每个候选版本都必须增加迁移数量”；应先比较候选与线上版本的迁移目录，无迁移改动时要求数量不变，有改动时才要求增加，否则普通代码热修会被误报为迁移失败。
- 2026-08-04: Wire 生成代码重排时，不能只确认服务对象“有被创建”，还要验证业务消费者“确实收到了同一个依赖”。对生图日志这类必需旁路，应用 Wire provider 强制注入，并在启动时 fail-fast，不能用 nil 时静默 return 掩盖部署回归。
- 2026-08-02: 异步接口的“接收容量”和“实际执行并发”必须分开设计。不能用进程内 `TryAcquire` 在任务落盘前拒绝第 N+1 个请求；应先持久化并返回 job_id，再由可恢复队列按用户/账号槽位执行。批量验收必须使用假上游，不能为了验证队列而制造真实生图费用或冲击账号。
- 2026-08-02: 新增 Redis 队列时不能让 handler 直接导入 Redis 客户端；队列能力应定义在 service，Redis 实现在 repository，handler 测试使用内存假实现。提交前用与 CI 相同版本的 gofmt、golangci-lint 和 Wire 生成器本机预检，避免把分层、格式和生成代码问题留到远端才发现。
- 2026-05-24: 本地没有系统级 `go`，后端验证不要默认临时下载 Go 工具链。除非需要本地复现后端问题，否则优先依赖 GitHub Actions 跑 Go 测试和安全扫描，本地只做代码审查、前端检查和必要的轻量命令。
- 2026-06-01: 2核4G 生产服务器不能承担 Sub2API Docker 源码构建，前端 `pnpm run build` 会把内存和 CPU 打满，导致 SSH/nginx 握手超时。部署必须改为 GitHub Actions 构建镜像，服务器只执行镜像加载和容器重启；部署 workflow 默认只手动触发。
- 2026-06-01: 合并官方 Sub2API 新版本时，必须显式检查私有定制路由是否还在，尤其是 `/v1/image-jobs/*` 异步生图接口。升级验收不能只看 `/health` 和首页 200，还要跑定制路由注册测试与线上探针。
- 2026-06-01: 服务器磁盘紧张时优先清 Docker BuildKit 构建缓存和系统旧日志，不要先碰 Postgres/Redis、账号池、图片日志和当前运行镜像。`docker builder prune -af` 可释放本机历史构建缓存，但正式部署已改为 GitHub Actions 构建，所以服务器不应依赖这些缓存。
- 2026-06-01: 生图文件增长主要来自本地文件存储，不是数据库本身。Sub2API 的 `image_logs` 保存原图/缩略图，`image_jobs` 的 `result.json` 可能保存图片响应体，ChatGPT2API 也会保存一份图片；排查磁盘时必须把这三类分开看。
- 2026-06-01: 服务器存储治理优先用保留天数和轻量 systemd timer 收口，避免为清理文件去服务器本地构建镜像。Sub2API 生图日志属于业务可视日志，异步 image-jobs 和 ChatGPT2API 图片属于短期缓存，保留策略应分开。
- 2026-07-16: 从官方 tag 重建私有分支时，不能只看“主要功能”提交；还要对照上一条真实线上分支与旧 `main` 的完整私有提交清单，特别检查账号空间展示、图片协议兼容和 Nginx 大文件配置。旧部署文件不能机械覆盖新配置。
- 2026-07-16: GitHub Actions 构建并不自动等于安全上线。生产部署还应在加载新镜像前标记旧镜像，并在健康检查、Nginx 校验或私有路由探针失败时自动恢复旧镜像。
- 2026-07-27: 解释数据结构变化前必须同时核对运行模式和前端入口；“数据库新增分组”不等于“管理后台直接可见”。SIMPLE 模式会隐藏分组管理页，但活跃公开分组仍可能出现在 API Key、账号和用量筛选中，因此升级测试必须验证真实可见入口和空分组影响。
- 2026-07-27: 用“是否有历史数据”判断全新安装时，普通 ORM 查询可能自动隐藏软删除记录。安装状态判断必须显式包含软删除历史，否则会把操作员曾经删除过数据的系统误判成空库并重新创建默认数据。
- 2026-07-27: 针对共享数据库的集成测试，单独运行目标用例通过并不能证明完整测试包会通过。测试“全新安装”时必须在当前事务中重建最小数据库状态，防止其他测试遗留的数据改变安装状态判断。
- 2026-07-27: 并发测试不能假定“启动第二个 goroutine”就等于它已经加入 singleflight。若调度器让它在首个请求完成后才运行，业务层的防重复限流就是合法结果；测试应同时覆盖复用进行中结果与完成后限流这两种有效时序。
- 2026-08-10: 公开版发布说明不能代替实际代码默认值审计。v0.1.173 文案说 Grok 跨客户端模型映射默认关闭，标签代码却把缺失值当成开启；私有升级必须把此类默认值改成严格显式启用，并用接口契约测试锁住。
- 2026-08-10: 生产数据库恢复到隔离演练环境后，数据内仍可能包含真实 OAuth 凭据。仅仅“不连生产数据库”不够，候选和旧版应用容器还必须运行在 `--internal` 无外网 Docker 网络，避免后台刷新/探测触达真实上游。
- 2026-08-10: “旧二进制能在新 schema 上启动”只是兼容性检查，不是数据库回滚。有数据改写迁移时，演练必须真正恢复升级前 dump，用旧版启动，再次升级，并单独比对被改写字段的备份和恢复结果。
- 2026-08-10: Grok 视频的计费发生在客户端轮询到终态后；如果客户端只提交不轮询，就可能漏扣。未做服务端持久轮询和补结算前，可以保留路由但不应对外宣传视频为完整可靠的商用能力。
